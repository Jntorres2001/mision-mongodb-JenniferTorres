use("bestiario");

// Inserción inválida (rango incorrecto y contraseña sin número)
db.guardianes.insertOne({
  nombre: "Lyra",
  rango: "Novato", // ❌ no está en el enum permitido
  password_acceso: "password", // ❌ no cumple el patrón de mayúscula + número
  inventario: [
    { nombre_item: "Varita", cantidad: 2 }
  ]
});
